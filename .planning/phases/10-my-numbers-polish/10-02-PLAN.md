---
phase: 10-my-numbers-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/components/myNumbers/IncrementalTab.tsx
  - src/components/myNumbers/RenewalTab.tsx
  - src/components/myNumbers/AddonsSection.tsx
  - src/components/myNumbers/EarningsSummary.tsx
  - src/components/myNumbers/MyNumbersChart.tsx
  - src/components/myNumbers/MyNumbersTrendsTab.tsx
  - src/components/myNumbers/SummaryCardRow.tsx
  - src/components/myNumbers/SettingsDialog.tsx
  - src/test/myNumbers/MyNumbersTrendsTab.test.tsx
  - src/pages/MyNumbersPage.tsx
autonomous: true
requirements:
  - NUM-05
  - NUM-07
must_haves:
  truths:
    - "MyNumbersPage.tsx is ≤400 lines and contains only: imports, OWNER_EMAILS const, useEffect-based owner gate, top-level state hooks (entries, settings, addons, showSettings, showAddOns, activeTab, expandedQuarter), pipelineByMonth memo, save/saveSettings/saveAddOns/updateEntry callbacks, derived calc memos, ytdTotals memo, attainment/pace derivations, and JSX layout that mounts the new sub-components"
    - "A new 'Trends' tab is added to the Tabs row at MyNumbersPage; its TabsContent mounts <MyNumbersTrendsTab entries={entries} pipelineByMonth={pipelineByMonth} settings={settings} incrementalCalcs={incrementalCalcs} />"
    - "Trends tab renders three vertically stacked recharts <ResponsiveContainer> charts with shared XAxis (formatMonth(e.month)) and y-axis fontSize=11: (1) Quota Attainment % over time — single Line on cumBookings/cumQuota×100, with <ReferenceLine y={100}>, (2) Activity Rate over time — dual Line on Meetings + Touches (no second yAxis; both are counts), (3) Pipeline Coverage over time — single Line on monthly pipeline ACV ÷ monthly quota, with <ReferenceLine y={3}> dashed"
    - "Sub-components for the existing tab content exist as separate files: IncrementalTab (the entire incremental table), RenewalTab (the entire renewal quarter table + large-renewal-addon callout), AddonsSection (the toggle + 3-column add-on cards), EarningsSummary (the FY27 Total Variable Compensation rollup), MyNumbersChart (the existing Bookings vs Quota chart), SummaryCardRow (the 4 summary cards at the top), SettingsDialog (the settings modal). Each is in src/components/myNumbers/, named exported, and props-driven (no internal state for shared data)"
    - "The page coordinator owns all state (entries, settings, addons, activeTab, expandedQuarter, showSettings, showAddOns) and passes typed props down; no sub-component reads localStorage or owns the comp memos"
    - "src/test/myNumbers/MyNumbersTrendsTab.test.tsx has ≥3 active render tests: smoke render with 12 entries renders three chart container elements; activity chart data shape matches input meetings/touches; coverage chart computes pipeline÷quota correctly when both > 0"
    - "bunx vitest run passes; npx tsc --noEmit clean; vite build clean; full suite stays green (no regressions in 10-01 tests or any prior phase tests)"
    - "Behavior of Incremental tab and Renewal tab is unchanged: same numbers, same edit cells, same expand/collapse on quarters, same large-renewal-addon callout"
    - "Switching tabs (Incremental / Renewal / Trends) preserves entries/settings/addons state in the coordinator — no re-load from localStorage"
  artifacts:
    - path: "src/components/myNumbers/IncrementalTab.tsx"
      provides: "The Incremental ACV tab content — props: { entries, incrementalCalcs, pipelineByMonth, ytdTotals, incrAttainment, onUpdateEntry: (month, field, value) => void }; renders the full Table with EditableCell rows + totals row"
      exports: ["IncrementalTab"]
      min_lines: 80
    - path: "src/components/myNumbers/RenewalTab.tsx"
      provides: "The Renewal ACV tab content — props: { entries, renewalCalcs, pipelineByMonth, ytdTotals, renewalRetention, largeRenewalAddon, expandedQuarter, onToggleQuarter, onUpdateEntry }; renders the quarter-grouped Table with expand/collapse + large-renewal-addon callout"
      exports: ["RenewalTab"]
      min_lines: 110
    - path: "src/components/myNumbers/AddonsSection.tsx"
      provides: "The Add-ons & SPIFFs section — props: { addons, addonPayouts, isOpen, onToggle, onSave: (next: AddOns) => void }; renders the chevron toggle + 3-column input cards"
      exports: ["AddonsSection"]
      min_lines: 70
    - path: "src/components/myNumbers/EarningsSummary.tsx"
      provides: "The FY27 Total Variable Compensation rollup card — props: { ytdTotals, annualAccel, largeRenewalAddon, addonPayouts }; pure read-only render"
      exports: ["EarningsSummary"]
      min_lines: 40
    - path: "src/components/myNumbers/MyNumbersChart.tsx"
      provides: "The existing Bookings vs Quota chart — props: { chartData: Array<{ month, Bookings, Quota, Payout }> }; renders the same recharts LineChart that lived inline at MyNumbersPage.tsx:778-791"
      exports: ["MyNumbersChart"]
      min_lines: 25
    - path: "src/components/myNumbers/MyNumbersTrendsTab.tsx"
      provides: "NEW Trends tab content — props: { entries, pipelineByMonth, settings, incrementalCalcs }; renders three stacked recharts (Attainment %, Activity Rate, Pipeline Coverage). Each chart has its own header label + ResponsiveContainer height=220"
      exports: ["MyNumbersTrendsTab"]
      min_lines: 90
    - path: "src/components/myNumbers/SummaryCardRow.tsx"
      provides: "The 4-card summary row at the top of the page — props: { ytdTotals, settings, incrAttainment, renewalRetention, projectedAttainment, projectedAnnual, monthlyPace }; mounts 4 SummaryCard children. Internal SummaryCard sub-component lives in this file"
      exports: ["SummaryCardRow"]
      min_lines: 60
    - path: "src/components/myNumbers/SettingsDialog.tsx"
      provides: "The Comp Plan Settings modal — props: { open, onOpenChange, settings, onSave: (next: CompSettings) => void }; renders the form with SettingsField rows + Reset Defaults / Done buttons. Internal SettingsField sub-component lives in this file"
      exports: ["SettingsDialog"]
      min_lines: 60
    - path: "src/test/myNumbers/MyNumbersTrendsTab.test.tsx"
      provides: "Vitest + @testing-library/react smoke tests — ≥3 cases: (1) renders 3 chart sections when given 12 entries, (2) Activity chart 'Meetings' line sees data from entries.meetings, (3) Coverage chart computes pipelineByMonth.incr.get(m)/entry.incrementalQuota correctly when both > 0"
      min_lines: 70
    - path: "src/pages/MyNumbersPage.tsx"
      provides: "Coordinator slimmed to ≤400 lines: state, owner gate, memos, callbacks, and JSX layout that mounts <SummaryCardRow />, the 3-tab Tabs (Incremental / Renewal / Trends), <AddonsSection />, <EarningsSummary />, <MyNumbersChart />, <SettingsDialog />. Inline JSX for tab content, summary cards, add-ons, earnings rollup, settings dialog, and chart all replaced with sub-component mounts"
      contains: "<MyNumbersTrendsTab"
  key_links:
    - from: "src/pages/MyNumbersPage.tsx"
      to: "src/components/myNumbers/MyNumbersTrendsTab.tsx"
      via: "import + mount inside <TabsContent value=\"trends\">"
      pattern: "<MyNumbersTrendsTab"
    - from: "src/pages/MyNumbersPage.tsx"
      to: "src/components/myNumbers/IncrementalTab.tsx + RenewalTab.tsx + SummaryCardRow.tsx + AddonsSection.tsx + EarningsSummary.tsx + MyNumbersChart.tsx + SettingsDialog.tsx"
      via: "named imports from @/components/myNumbers/* + JSX mounts inside the page"
      pattern: "from \"@/components/myNumbers/"
    - from: "src/components/myNumbers/MyNumbersTrendsTab.tsx"
      to: "recharts (LineChart, Line, ReferenceLine, ResponsiveContainer, XAxis, YAxis, CartesianGrid, Tooltip)"
      via: "import { LineChart, Line, ReferenceLine, ... } from \"recharts\""
      pattern: "from \"recharts\""
    - from: "src/components/myNumbers/MyNumbersTrendsTab.tsx"
      to: "src/data/myNumbers/storage.ts (NumbersEntry, CompSettings types)"
      via: "import type { NumbersEntry, CompSettings } from \"@/data/myNumbers/storage\""
      pattern: "from \"@/data/myNumbers/storage\""
    - from: "src/test/myNumbers/MyNumbersTrendsTab.test.tsx"
      to: "src/components/myNumbers/MyNumbersTrendsTab.tsx"
      via: "import { MyNumbersTrendsTab } from \"@/components/myNumbers/MyNumbersTrendsTab\""
      pattern: "MyNumbersTrendsTab"
---

<objective>
Extension pass for Phase 10: ship the CLAUDE.md priority #10 mandate ("Quota attainment, activity rate, pipeline coverage tracked over time") by adding a third **Trends** tab to MyNumbersPage with three new recharts visualizations. In the same plan, decompose the now ~530-580 line MyNumbersPage (post-10-01) into focused, props-driven sub-components in `src/components/myNumbers/` so the coordinator drops to ≤400 lines (matching the Phase 03 UX-04 ceiling). Sub-components: SummaryCardRow, IncrementalTab, RenewalTab, MyNumbersTrendsTab, AddonsSection, EarningsSummary, MyNumbersChart, SettingsDialog.

Purpose: closes the only outstanding CLAUDE.md priority roadmap item (#10) and applies the proven Phase 03 decomposition pattern. The current MyNumbersPage has one chart (Bookings vs Quota in dollars) but the page already captures meetings, touches, and pipeline-by-month — the data exists, it just isn't visualized over time. Three new charts surface that data in a clean, narrative-driven Trends tab. Decomposition is paired so each tab gets its own file and the page becomes a layout coordinator instead of a god-component.

Output:
- `src/components/myNumbers/SummaryCardRow.tsx` (4-card summary row + internal SummaryCard)
- `src/components/myNumbers/IncrementalTab.tsx` (Incremental tab content)
- `src/components/myNumbers/RenewalTab.tsx` (Renewal tab content + large-renewal callout)
- `src/components/myNumbers/MyNumbersTrendsTab.tsx` (NEW: 3 charts)
- `src/components/myNumbers/AddonsSection.tsx` (Add-ons & SPIFFs toggle + cards)
- `src/components/myNumbers/EarningsSummary.tsx` (FY27 Total Variable Compensation rollup)
- `src/components/myNumbers/MyNumbersChart.tsx` (existing Bookings vs Quota chart)
- `src/components/myNumbers/SettingsDialog.tsx` (settings modal + internal SettingsField)
- `src/test/myNumbers/MyNumbersTrendsTab.test.tsx` (3 smoke tests)
- Edits to `src/pages/MyNumbersPage.tsx` (coordinator only, ≤400 lines)

Estimated: 75-105 min agent execution. Pattern matches Phase 03 03-03-PLAN.md (TerritoryPlanner decomposition) and Phase 07 07-01-PLAN.md (component extraction). 10-01 RED tests stay green; new Trends tests are additive.

**Decomposition order** (per RESEARCH.md):
1. Leaves first (no cross-deps): SummaryCardRow, EarningsSummary, MyNumbersChart, MyNumbersTrendsTab
2. Mid-level: AddonsSection, SettingsDialog
3. Tab content (~100-150 lines each): IncrementalTab, RenewalTab
4. Coordinator slim-down: replace inline JSX with sub-component mounts; verify ≤400 lines

**Pipeline Coverage math (per RESEARCH.md Open Question #3):** monthly pipeline ÷ monthly quota; reference line at 3.0 dashed. This is sales-ops standard ("3x quota cover"). When monthly quota = 0, return 0 (avoid div-by-zero); chart still renders the line at 0 for that point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-my-numbers-polish/RESEARCH.md
@.planning/phases/10-my-numbers-polish/10-01-SUMMARY.md
@.planning/phases/03-component-decomposition-ux-polish/03-03-SUMMARY.md
@CLAUDE.md
@src/pages/MyNumbersPage.tsx

<interfaces>
<!-- Types the executor needs to pass between coordinator and sub-components -->

From src/data/myNumbers/storage.ts (created in 10-01):

```typescript
export interface NumbersEntry {
  month: string;
  incrementalQuota: number;
  incrementalBookings: number;
  renewedAcv: number;
  pipelineAcv: number;
  meetings: number;
  outreachTouches: number;
}

export interface CompSettings { /* ... */ }
export interface AddOns { /* ... */ }

export const FY27_MONTHS: string[];
export const DEFAULT_QUOTAS: Record<string, number>;
// ... loadEntries, loadSettings, loadAddOns
```

From src/data/myNumbers/comp.ts (created in 10-01):

```typescript
export function calcIncrementalForMonth(entries, monthIndex, settings): {
  baseCommission: number; ytdAccel: number; monthT1: number; monthT2: number; monthT3: number;
  ytdBookings: number; ytdQuota: number;
};
// ... + 5 other comp fns
```

From MyNumbersPage.tsx (post-10-01) — coordinator computes and owns these memos:

```typescript
incrementalCalcs: Array<ReturnType<typeof calcIncrementalForMonth>>;  // length 12
renewalCalcs: Array<ReturnType<typeof calcRenewalForMonth>>;           // length 12
addonPayouts: ReturnType<typeof calcAddOnPayouts>;
ytdTotals: { totalIncrBookings, totalIncrQuota, totalRenewed, totalBaseComm, totalYtdAccel,
             totalIncrPayout, totalRenewalPayout, totalEarnings, annualAccel: number };
pipelineByMonth: { incr: Map<string, number>; renew: Map<string, number> };
incrAttainment: number;       // 0..1+
renewalRetention: number;     // 0..1
projectedAttainment: number;
projectedAnnual: number;
monthlyPace: number;
annualAccel: number;
largeRenewalAddon: number;
```

These remain in the coordinator. Sub-components receive them as typed props.

**MyNumbersTrendsTab props contract:**

```typescript
interface MyNumbersTrendsTabProps {
  entries: NumbersEntry[];
  pipelineByMonth: { incr: Map<string, number>; renew: Map<string, number> };
  settings: CompSettings;
  incrementalCalcs: Array<{ baseCommission: number; ytdAccel: number; ytdBookings: number; ytdQuota: number; /* ... */ }>;
}
```

The component derives chart data internally:
- Attainment %: `(incrementalCalcs[i].ytdBookings / incrementalCalcs[i].ytdQuota) * 100` (or 0 if ytdQuota=0)
- Activity Rate: `{ Meetings: e.meetings, Touches: e.outreachTouches }`
- Pipeline Coverage: `(pipelineByMonth.incr.get(e.month) ?? 0) / (e.incrementalQuota || 1)` capped/floored sensibly; if quota=0, return 0
</interfaces>

<existing_pattern>
<!-- Phase 03 decomposition reference (TerritoryPlanner.tsx → ProspectFilterBar.tsx etc.) -->
<!-- Read .planning/phases/03-component-decomposition-ux-polish/03-03-PLAN.md for the exact pattern -->

Coordinator pattern:
- All state lives in the coordinator (useState, useMemo, useCallback)
- Sub-components are pure presentation: receive typed props, emit typed callbacks
- No localStorage reads in sub-components; coordinator owns persistence
- Named exports (`export function ComponentName(...)`) — never default exports for sub-components
</existing_pattern>

<chart_pattern>
<!-- Existing chart at MyNumbersPage.tsx:778-791 — mirror this exactly for the 3 new charts -->

```tsx
<ResponsiveContainer width="100%" height={250}>
  <LineChart data={chartData}>
    <CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))" />
    <XAxis dataKey="month" tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} />
    <YAxis tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} tickFormatter={v => ...} />
    <RechartsTooltip formatter={(value: number) => ...} />
    <Line type="monotone" dataKey="..." stroke="hsl(var(--primary))" strokeWidth={2.5} dot={{ r: 4 }} />
  </LineChart>
</ResponsiveContainer>
```

For the Trends tab, use height=220 per chart and stack with `space-y-6`. Use ReferenceLine where appropriate (y=100 on attainment, y=3 on coverage).
</chart_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED + scaffold): Create all 8 sub-components as stubs and Trends tab tests; mount Trends tab into MyNumbersPage with stub content</name>
  <files>
    src/components/myNumbers/SummaryCardRow.tsx
    src/components/myNumbers/IncrementalTab.tsx
    src/components/myNumbers/RenewalTab.tsx
    src/components/myNumbers/MyNumbersTrendsTab.tsx
    src/components/myNumbers/AddonsSection.tsx
    src/components/myNumbers/EarningsSummary.tsx
    src/components/myNumbers/MyNumbersChart.tsx
    src/components/myNumbers/SettingsDialog.tsx
    src/test/myNumbers/MyNumbersTrendsTab.test.tsx
    src/pages/MyNumbersPage.tsx
  </files>
  <behavior>
    All 8 sub-component files exist with named exports + typed props but minimal stub bodies (e.g., `export function IncrementalTab(props) { return <div>TODO IncrementalTab</div>; }`). Tests for MyNumbersTrendsTab fail until the component is implemented in this same task. The page coordinator imports the new MyNumbersTrendsTab and mounts it inside a third `<TabsTrigger value="trends">Trends</TabsTrigger>` + `<TabsContent value="trends">`. The other 7 sub-components are scaffolded but not yet wired into the page (page still uses inline JSX for incremental table, renewal table, summary cards, add-ons, earnings, chart, settings dialog).

    After this task, MyNumbersTrendsTab is real (not stubbed) — it renders 3 charts. The other 7 are stubs that will be wired in Task 2.
  </behavior>
  <action>
    **Step 1 — Create directory + 8 sub-component scaffolds:**

    `src/components/myNumbers/SummaryCardRow.tsx`:
    ```typescript
    import type { CompSettings } from "@/data/myNumbers/storage";
    interface SummaryCardRowProps {
      ytdTotals: { totalIncrBookings: number; totalIncrQuota: number; totalRenewed: number;
                   totalIncrPayout: number; totalRenewalPayout: number; totalEarnings: number };
      settings: CompSettings;
      incrAttainment: number;
      renewalRetention: number;
      projectedAttainment: number;
      projectedAnnual: number;
      monthlyPace: number;
    }
    export function SummaryCardRow(props: SummaryCardRowProps) {
      return <div data-testid="summary-card-row-stub">TODO SummaryCardRow</div>;
    }
    ```

    Repeat for `IncrementalTab`, `RenewalTab`, `AddonsSection`, `EarningsSummary`, `MyNumbersChart`, `SettingsDialog`. For each, declare the props interface from the <interfaces> block above and stub the body.

    **Step 2 — Implement MyNumbersTrendsTab fully (this is the only "real" component in Task 1):**

    `src/components/myNumbers/MyNumbersTrendsTab.tsx`:
    ```typescript
    import { useMemo } from "react";
    import { LineChart, Line, XAxis, YAxis, CartesianGrid,
             Tooltip as RechartsTooltip, ResponsiveContainer, ReferenceLine } from "recharts";
    import type { NumbersEntry, CompSettings } from "@/data/myNumbers/storage";

    function formatMonth(ym: string): string {
      const [y, m] = ym.split("-");
      const date = new Date(parseInt(y), parseInt(m) - 1);
      return date.toLocaleDateString("en-US", { month: "short", year: "numeric" });
    }

    interface MyNumbersTrendsTabProps {
      entries: NumbersEntry[];
      pipelineByMonth: { incr: Map<string, number>; renew: Map<string, number> };
      settings: CompSettings;
      incrementalCalcs: Array<{ ytdBookings: number; ytdQuota: number }>;
    }

    export function MyNumbersTrendsTab({ entries, pipelineByMonth, settings, incrementalCalcs }: MyNumbersTrendsTabProps) {
      const attainmentData = useMemo(() =>
        entries.map((e, i) => ({
          month: formatMonth(e.month),
          Attainment: incrementalCalcs[i].ytdQuota > 0
            ? Math.round((incrementalCalcs[i].ytdBookings / incrementalCalcs[i].ytdQuota) * 100)
            : 0,
        })), [entries, incrementalCalcs]);

      const activityData = useMemo(() =>
        entries.map(e => ({
          month: formatMonth(e.month),
          Meetings: e.meetings,
          Touches: e.outreachTouches,
        })), [entries]);

      const coverageData = useMemo(() =>
        entries.map(e => {
          const pipeline = pipelineByMonth.incr.get(e.month) ?? 0;
          const ratio = e.incrementalQuota > 0 ? pipeline / e.incrementalQuota : 0;
          return { month: formatMonth(e.month), Coverage: Number(ratio.toFixed(2)) };
        }), [entries, pipelineByMonth]);

      return (
        <div className="space-y-6" data-testid="trends-tab">
          {/* Attainment % */}
          <div className="rounded-lg border border-border p-5 bg-card" data-testid="trends-attainment">
            <h2 className="text-sm font-bold text-foreground uppercase tracking-wider mb-4">Quota Attainment % (cumulative YTD)</h2>
            <ResponsiveContainer width="100%" height={220}>
              <LineChart data={attainmentData}>
                <CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))" />
                <XAxis dataKey="month" tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} />
                <YAxis tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} tickFormatter={v => `${v}%`} />
                <RechartsTooltip formatter={(value: number) => `${value}%`} />
                <ReferenceLine y={100} stroke="hsl(var(--muted-foreground))" strokeDasharray="6 3" label={{ value: "100%", fontSize: 10, fill: "hsl(var(--muted-foreground))" }} />
                <Line type="monotone" dataKey="Attainment" stroke="hsl(var(--primary))" strokeWidth={2.5} dot={{ r: 4 }} />
              </LineChart>
            </ResponsiveContainer>
          </div>

          {/* Activity Rate */}
          <div className="rounded-lg border border-border p-5 bg-card" data-testid="trends-activity">
            <h2 className="text-sm font-bold text-foreground uppercase tracking-wider mb-4">Activity Rate (per month)</h2>
            <ResponsiveContainer width="100%" height={220}>
              <LineChart data={activityData}>
                <CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))" />
                <XAxis dataKey="month" tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} />
                <YAxis tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} />
                <RechartsTooltip />
                <Line type="monotone" dataKey="Meetings" stroke="hsl(var(--primary))" strokeWidth={2.5} dot={{ r: 4 }} />
                <Line type="monotone" dataKey="Touches" stroke="hsl(var(--muted-foreground))" strokeWidth={2} dot={{ r: 3 }} strokeDasharray="3 3" />
              </LineChart>
            </ResponsiveContainer>
          </div>

          {/* Pipeline Coverage */}
          <div className="rounded-lg border border-border p-5 bg-card" data-testid="trends-coverage">
            <h2 className="text-sm font-bold text-foreground uppercase tracking-wider mb-4">Pipeline Coverage (monthly pipeline ÷ monthly quota)</h2>
            <ResponsiveContainer width="100%" height={220}>
              <LineChart data={coverageData}>
                <CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))" />
                <XAxis dataKey="month" tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} />
                <YAxis tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }} tickFormatter={v => `${v.toFixed(1)}x`} />
                <RechartsTooltip formatter={(value: number) => `${value.toFixed(2)}x`} />
                <ReferenceLine y={3} stroke="hsl(var(--muted-foreground))" strokeDasharray="6 3" label={{ value: "3x target", fontSize: 10, fill: "hsl(var(--muted-foreground))" }} />
                <Line type="monotone" dataKey="Coverage" stroke="hsl(var(--primary))" strokeWidth={2.5} dot={{ r: 4 }} />
              </LineChart>
            </ResponsiveContainer>
          </div>
        </div>
      );
    }
    ```

    Note: `settings` prop is in the signature for future expansion (e.g., target attainment line) but unused in this version. Mark with `// eslint-disable-next-line @typescript-eslint/no-unused-vars` if ESLint complains, OR simply drop `settings` from the props until needed. Recommendation: drop it; YAGNI.

    **Step 3 — Test scaffold:**

    `src/test/myNumbers/MyNumbersTrendsTab.test.tsx`:
    ```typescript
    import { describe, it, expect } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { MyNumbersTrendsTab } from "@/components/myNumbers/MyNumbersTrendsTab";
    import { FY27_MONTHS, DEFAULT_QUOTAS, type NumbersEntry } from "@/data/myNumbers/storage";

    function makeEntries(overrides: Partial<Record<string, Partial<NumbersEntry>>> = {}): NumbersEntry[] {
      return FY27_MONTHS.map(m => ({
        month: m,
        incrementalQuota: DEFAULT_QUOTAS[m] ?? 0,
        incrementalBookings: 0,
        renewedAcv: 0,
        pipelineAcv: 0,
        meetings: 0,
        outreachTouches: 0,
        ...overrides[m],
      }));
    }

    function makeIncrementalCalcs(entries: NumbersEntry[]) {
      let cumB = 0, cumQ = 0;
      return entries.map(e => {
        cumB += e.incrementalBookings;
        cumQ += e.incrementalQuota;
        return { ytdBookings: cumB, ytdQuota: cumQ, baseCommission: 0, ytdAccel: 0, monthT1: 0, monthT2: 0, monthT3: 0 };
      });
    }

    describe("MyNumbersTrendsTab", () => {
      it("renders three chart sections", () => {
        const entries = makeEntries();
        render(<MyNumbersTrendsTab
          entries={entries}
          pipelineByMonth={{ incr: new Map(), renew: new Map() }}
          incrementalCalcs={makeIncrementalCalcs(entries)}
        />);
        expect(screen.getByTestId("trends-attainment")).toBeInTheDocument();
        expect(screen.getByTestId("trends-activity")).toBeInTheDocument();
        expect(screen.getByTestId("trends-coverage")).toBeInTheDocument();
      });

      it("renders chart headers", () => {
        const entries = makeEntries();
        render(<MyNumbersTrendsTab
          entries={entries}
          pipelineByMonth={{ incr: new Map(), renew: new Map() }}
          incrementalCalcs={makeIncrementalCalcs(entries)}
        />);
        expect(screen.getByText(/Quota Attainment/i)).toBeInTheDocument();
        expect(screen.getByText(/Activity Rate/i)).toBeInTheDocument();
        expect(screen.getByText(/Pipeline Coverage/i)).toBeInTheDocument();
      });

      it("does not crash when entries have zero quotas (avoids div-by-zero on coverage chart)", () => {
        const entries = makeEntries({ "2026-02": { incrementalQuota: 0, incrementalBookings: 0 } });
        const calcs = makeIncrementalCalcs(entries);
        // Should not throw
        expect(() => render(<MyNumbersTrendsTab
          entries={entries}
          pipelineByMonth={{ incr: new Map([["2026-02", 5000]]), renew: new Map() }}
          incrementalCalcs={calcs}
        />)).not.toThrow();
      });
    });
    ```

    Note: drop the `settings` prop from the call sites if you removed it from the component signature in Step 2.

    **Step 4 — Wire only Trends tab into MyNumbersPage (not the other 7 sub-components yet):**

    Add the import:
    ```typescript
    import { MyNumbersTrendsTab } from "@/components/myNumbers/MyNumbersTrendsTab";
    ```

    Modify the Tabs section at MyNumbersPage.tsx ~:499-503:
    ```tsx
    <Tabs value={activeTab} onValueChange={setActiveTab}>
      <TabsList className="mb-3">
        <TabsTrigger value="incremental">Incremental ACV</TabsTrigger>
        <TabsTrigger value="renewal">Renewal ACV</TabsTrigger>
        <TabsTrigger value="trends">Trends</TabsTrigger>
      </TabsList>

      <TabsContent value="incremental">{/* existing inline JSX unchanged */}</TabsContent>
      <TabsContent value="renewal">{/* existing inline JSX unchanged */}</TabsContent>
      <TabsContent value="trends">
        <MyNumbersTrendsTab
          entries={entries}
          pipelineByMonth={pipelineByMonth}
          incrementalCalcs={incrementalCalcs}
        />
      </TabsContent>
    </Tabs>
    ```

    **Step 5 — Verify Trends tab works end-to-end:**

    Run:
    ```
    bunx vitest run myNumbers
    npx tsc --noEmit
    bun run dev   # smoke: navigate to /my-numbers, click Trends tab, see 3 charts
    ```

    All Trends tests should pass; 10-01 tests stay green; tsc is clean. Manual smoke shows 3 charts render in the new tab.

    Constraints:
    - Do NOT replace any of the 7 inline JSX blocks (incremental table, renewal table, summary cards, add-ons, earnings rollup, chart, settings dialog) yet — leave them inline. Task 2 does the wiring.
    - Sub-component stubs are placeholder-only; they're not used yet.
    - Do NOT add tests for stub components (they're stubs, no behavior to assert).
  </action>
  <verify>
    <automated>bunx vitest run myNumbers 2>&amp;1 | tail -10 &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - 8 sub-component files exist under `src/components/myNumbers/` with typed props
    - 7 of them are stubs (return placeholder div); 1 (MyNumbersTrendsTab) is fully implemented
    - `src/test/myNumbers/MyNumbersTrendsTab.test.tsx` has ≥3 tests, all green
    - MyNumbersPage Tabs has 3 triggers (Incremental / Renewal / Trends); Trends tab mounts MyNumbersTrendsTab
    - Manual smoke at /my-numbers → click Trends → 3 charts render with current data
    - 10-01 tests stay green (full vitest run passes)
    - `npx tsc --noEmit` clean
  </done>
</task>

<task type="auto">
  <name>Task 2 (GREEN + slim-down): Replace inline JSX with sub-component mounts; coordinator drops to ≤400 lines</name>
  <files>
    src/components/myNumbers/SummaryCardRow.tsx
    src/components/myNumbers/IncrementalTab.tsx
    src/components/myNumbers/RenewalTab.tsx
    src/components/myNumbers/AddonsSection.tsx
    src/components/myNumbers/EarningsSummary.tsx
    src/components/myNumbers/MyNumbersChart.tsx
    src/components/myNumbers/SettingsDialog.tsx
    src/pages/MyNumbersPage.tsx
  </files>
  <behavior>
    The page coordinator (MyNumbersPage.tsx) is ≤400 lines. The 7 sub-component stubs from Task 1 are replaced with their real implementations, lifted from the corresponding inline JSX in MyNumbersPage. Page renders identically to pre-task: same numbers, same edit cells, same toggles, same chart. State (entries, settings, addons, activeTab, expandedQuarter, showSettings, showAddOns) still lives in the coordinator.
  </behavior>
  <action>
    Decompose in dependency order (leaves first). For each sub-component: copy the corresponding inline JSX from MyNumbersPage.tsx into the sub-component file, replace any closure-captured state with prop names, replace any closure-captured callbacks with prop callbacks. Then in MyNumbersPage.tsx, replace the inline JSX with the sub-component mount.

    **Suggested commit sequence (one per sub-component for easy review):**

    ### Sub-component 1: SummaryCardRow

    Move from MyNumbersPage.tsx:465-496 (the 4 SummaryCard mounts). Also move the inline `SummaryCard` function definition at MyNumbersPage.tsx:825-859 INTO this file (it's only used by SummaryCardRow).

    Props in: `ytdTotals, settings, incrAttainment, renewalRetention, projectedAttainment, projectedAnnual, monthlyPace`
    Internal: `function SummaryCard(...) { ... }` with the same signature/body as the existing inline one.

    In page coordinator: replace lines 465-496 with `<SummaryCardRow ytdTotals={ytdTotals} settings={settings} incrAttainment={incrAttainment} renewalRetention={renewalRetention} projectedAttainment={projectedAttainment} projectedAnnual={projectedAnnual} monthlyPace={monthlyPace} />`. Delete inline SummaryCard function.

    ### Sub-component 2: MyNumbersChart

    Move from MyNumbersPage.tsx:778-791 (the entire chart card div). Helper `formatMonth` is needed; if not yet exported from storage, copy a private copy into the component file (same as the Trends tab does).

    Props in: `chartData: Array<{ month: string; Bookings: number; Quota: number; Payout: number }>`. The coordinator already computes `chartData` via useMemo at MyNumbersPage.tsx:424-432; that memo stays in the coordinator and gets passed down.

    In page coordinator: replace lines 778-791 with `<MyNumbersChart chartData={chartData} />`.

    ### Sub-component 3: EarningsSummary

    Move from MyNumbersPage.tsx:739-776 (the FY27 Total Variable Compensation rollup card).

    Props in: `ytdTotals, annualAccel, largeRenewalAddon, addonPayouts`. Pure render.

    In page coordinator: replace lines 739-776 with `<EarningsSummary ytdTotals={ytdTotals} annualAccel={annualAccel} largeRenewalAddon={largeRenewalAddon} addonPayouts={addonPayouts} />`.

    ### Sub-component 4: AddonsSection

    Move from MyNumbersPage.tsx:687-737 (the toggle button + 3-column input cards).

    Props in: `addons, addonPayouts, isOpen: boolean, onToggle: () => void, onSave: (next: AddOns) => void`.

    In page coordinator: replace lines 687-737 with `<AddonsSection addons={addons} addonPayouts={addonPayouts} isOpen={showAddOns} onToggle={() => setShowAddOns(!showAddOns)} onSave={saveAddOns} />`.

    ### Sub-component 5: SettingsDialog

    Move from MyNumbersPage.tsx:794-818 (the Dialog + DialogContent block) AND move the inline `SettingsField` function definition at MyNumbersPage.tsx:861-874 INTO this file (only used by SettingsDialog).

    Props in: `open: boolean, onOpenChange: (open: boolean) => void, settings: CompSettings, onSave: (next: CompSettings) => void`.

    In page coordinator: replace lines 794-818 with `<SettingsDialog open={showSettings} onOpenChange={setShowSettings} settings={settings} onSave={saveSettings} />`. Delete inline SettingsField function.

    ### Sub-component 6: IncrementalTab

    Move from MyNumbersPage.tsx:505-576 (the entire `<TabsContent value="incremental">` block — the table, the EditableCell rows, the totals row).

    Props in: `entries, incrementalCalcs, pipelineByMonth, ytdTotals, incrAttainment, onUpdateEntry: (month: string, field: keyof NumbersEntry, value: number) => void`.

    EditableCell stays in MyNumbersPage.tsx for now (RenewalTab also uses it). Pass down via prop reference, OR extract EditableCell to its own file (`src/components/myNumbers/EditableCell.tsx`) and import in both tabs. Recommendation: extract EditableCell to its own file in this commit; both tabs import it. This is a tiny extra step but correct.

    `src/components/myNumbers/EditableCell.tsx`: copy from MyNumbersPage.tsx:292-327 verbatim (preserving the `aria-label` prop added in 10-01).

    In page coordinator: replace lines 505-576 with `<IncrementalTab entries={entries} incrementalCalcs={incrementalCalcs} pipelineByMonth={pipelineByMonth} ytdTotals={ytdTotals} incrAttainment={incrAttainment} onUpdateEntry={updateEntry} />`. Delete inline EditableCell function.

    ### Sub-component 7: RenewalTab

    Move from MyNumbersPage.tsx:578-684 (the entire `<TabsContent value="renewal">` block — the quarter table, the IIFE, the large-renewal-addon callout).

    Props in: `entries, renewalCalcs, pipelineByMonth, ytdTotals, renewalRetention, largeRenewalAddon, expandedQuarter: string | null, onToggleQuarter: (label: string | null) => void, onUpdateEntry`.

    The IIFE that builds the quarter rows can stay inside the component as a derived constant or stay as an IIFE — preserve verbatim if possible to minimize risk. The dead-code-shadow `FY27_MONTHS` array at line 602 was deleted in 10-01; if it crept back, delete again — RenewalTab should import FY27_MONTHS from `@/data/myNumbers/storage`.

    In page coordinator: replace lines 578-684 with `<RenewalTab entries={entries} renewalCalcs={renewalCalcs} pipelineByMonth={pipelineByMonth} ytdTotals={ytdTotals} renewalRetention={renewalRetention} largeRenewalAddon={largeRenewalAddon} expandedQuarter={expandedQuarter} onToggleQuarter={setExpandedQuarter} onUpdateEntry={updateEntry} />`.

    ### Final coordinator structure

    After all 7 wirings, MyNumbersPage.tsx should contain only:
    1. Imports (React, hooks, storage, comp, sub-components, lucide icons, ui Button + Tabs)
    2. `OWNER_EMAILS` const
    3. `MyNumbersPage` function:
       - Hooks: navigate, useAuth, useTerritories, useOpportunities, useState ×7, useEffect (owner gate)
       - useMemo: pipelineByMonth, incrementalCalcs, renewalCalcs, annualAccel, largeRenewalAddon, addonPayouts, ytdTotals, chartData
       - useCallback: save, saveSettings, saveAddOns, updateEntry
       - Derived: incrAttainment, renewalRetention, activeMonths, monthlyPace, projectedAnnual, projectedAttainment
       - JSX: header bar + `<SummaryCardRow />` + `<Tabs>` containing `<IncrementalTab />`, `<RenewalTab />`, `<MyNumbersTrendsTab />` + `<AddonsSection />` + `<EarningsSummary />` + `<MyNumbersChart />` + `<SettingsDialog />`

    Verify line count: `wc -l src/pages/MyNumbersPage.tsx` must be ≤400.

    **Final verification:**

    ```
    bunx vitest run                          # full suite green
    npx tsc --noEmit                         # clean
    bun run build                            # clean
    wc -l src/pages/MyNumbersPage.tsx        # ≤400
    ```

    Manual smoke:
    1. `bun run dev`, open /my-numbers
    2. Verify: same 4 summary cards, same Incremental tab numbers, same Renewal quarters, same Settings dialog, same chart
    3. Click Trends tab → 3 charts render
    4. Edit a quota cell → number updates → reload → persists
    5. Open Settings → change Annual TI → Done → number updates immediately

    Constraints:
    - One sub-component per commit if possible. After each commit, run `bunx vitest run` to confirm green. If anything regresses, debug before moving to the next sub-component.
    - Do NOT change any comp math, any localStorage write logic, or any state structure. This is pure JSX-relocation.
    - The file list in `<files>` is the final state; if the executor extracts EditableCell to its own file, add `src/components/myNumbers/EditableCell.tsx` to the actual commit.
  </action>
  <verify>
    <automated>bunx vitest run 2>&amp;1 | tail -10 &amp;&amp; npx tsc --noEmit &amp;&amp; bun run build 2>&amp;1 | tail -5 &amp;&amp; LINES=$(wc -l &lt; src/pages/MyNumbersPage.tsx); echo "MyNumbersPage lines: $LINES"; [ $LINES -le 400 ] &amp;&amp; echo "OK ≤400" || echo "FAIL: over 400 lines"</automated>
  </verify>
  <done>
    - All 7 sub-component files have real implementations (no more "TODO" stubs)
    - MyNumbersPage.tsx is ≤400 lines (`wc -l` confirms)
    - Full Vitest suite green (10-01 tests + 10-02 Trends tests + all prior phase tests)
    - `npx tsc --noEmit` clean
    - `bun run build` clean
    - Manual smoke at /my-numbers shows identical behavior across all 3 tabs + Settings + Add-ons + chart
    - The coordinator JSX is a clean composition of sub-component mounts (no inline tables, no inline cards, no inline dialogs)
  </done>
</task>

</tasks>

<verification>

**Automated:**
- `bunx vitest run` — full suite green
- `bunx vitest run myNumbers/MyNumbersTrendsTab` — ≥3 tests green
- `npx tsc --noEmit` — clean
- `bun run build` — clean
- `wc -l src/pages/MyNumbersPage.tsx` — ≤400
- `ls src/components/myNumbers/` — at least 8 files (SummaryCardRow, IncrementalTab, RenewalTab, MyNumbersTrendsTab, AddonsSection, EarningsSummary, MyNumbersChart, SettingsDialog; possibly EditableCell)

**Manual UAT (owner flow only):**
1. Open `/my-numbers`; verify summary cards + Incremental tab + Renewal tab + chart all match pre-task behavior
2. Click **Trends** tab → 3 charts render: Quota Attainment %, Activity Rate, Pipeline Coverage
3. Verify Attainment chart shows monthly cumulative attainment %, with reference line at 100%
4. Verify Activity chart shows Meetings (solid line) + Touches (dashed line)
5. Verify Coverage chart shows monthly pipeline ÷ monthly quota ratio with reference line at 3x
6. Edit a Quota cell in Incremental → tab → reload → number persists; switch to Trends → chart reflects new attainment
7. Open Settings → change Annual TI → Done → numbers update immediately

</verification>

<success_criteria>

The plan is complete when:
1. NUM-05 must_haves pass: Trends tab exists with 3 charts (Attainment %, Activity Rate, Pipeline Coverage)
2. NUM-07 must_haves pass: MyNumbersPage.tsx ≤400 lines after extraction; 7 sub-components live in src/components/myNumbers/
3. Behavior of all existing tabs/cards/dialogs is unchanged (no number drift, no UX regression)
4. Test coverage for Trends tab is in place (≥3 cases)
5. Full vitest suite is green; tsc clean; build clean
6. Coordinator owns all state; sub-components are pure presentation with typed props
7. Phase 10 closes CLAUDE.md priority roadmap item #10 ("Quota attainment, activity rate, pipeline coverage tracked over time")

</success_criteria>

<output>
After completion, create `.planning/phases/10-my-numbers-polish/10-02-SUMMARY.md` documenting:
- Final MyNumbersPage line count (target ≤400)
- The 8 sub-components created (file paths + line counts)
- Trends tab UAT confirmation: which charts render, any visual issues, whether the 3x reference line on Coverage chart matches Micah's expectation
- Open follow-ups (e.g., NUM-V2 candidates: settable activity targets, smart-default tab when attainment <70%, second yAxis for percentage charts, Trends tab tests for Attainment + Coverage data shape; competitor pressure trend; per-stage forecast trend)
- Phase 10 wrap-up: status of all 8 NUM requirements (NUM-01..04, NUM-06, NUM-08 closed by 10-01; NUM-05, NUM-07 closed by 10-02)
</output>
