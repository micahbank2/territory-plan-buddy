---
phase: 03-component-decomposition-ux-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/ProspectSheet.tsx
  - src/components/TerritoryPlanner.tsx
  - src/components/ProspectPage.tsx
  - src/test/ProspectSheet.tab.test.tsx
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "ProspectSheet displays four tabs labeled Overview, Activity, Contacts, Tasks"
    - "Overview tab is the default when a prospect sheet opens"
    - "User can click a tab and its panel becomes the visible content"
    - "When the user switches to a different prospect while the sheet stays open, the active tab persists (not reset to Overview)"
    - "When the sheet closes and reopens, the active tab resets to Overview"
  artifacts:
    - path: "src/components/ProspectSheet.tsx"
      provides: "Tabs container with four TabsContent panels; accepts activeTab+onTabChange controlled props"
      contains: "TabsList"
    - path: "src/components/TerritoryPlanner.tsx"
      provides: "sheetTab useState lifted to coordinator; handleSheetClose resets it to 'overview'"
      contains: "sheetTab"
    - path: "src/test/ProspectSheet.tab.test.tsx"
      provides: "Passing tests for UX-01 (4 tabs present) and UX-02 (persistence across prospect switch, reset on close)"
  key_links:
    - from: "src/components/TerritoryPlanner.tsx"
      to: "src/components/ProspectSheet.tsx"
      via: "activeTab and onTabChange props passed down, sheetTab reset inside handleSheetClose"
      pattern: "activeTab=\\{sheetTab\\}"
    - from: "src/components/ProspectSheet.tsx"
      to: "src/components/ui/tabs.tsx (shadcn Tabs)"
      via: "controlled Tabs value + onValueChange wired to props"
      pattern: "<Tabs\\s+value="
---

<objective>
Introduce the tabbed IA in ProspectSheet (UX-01) with correctly persisted tab state at the coordinator level (UX-02). Four tabs: Overview, Activity, Contacts, Tasks. Distribute existing sections per the research map. Tab state is a controlled prop from TerritoryPlanner so switching prospects keeps the tab sticky and closing the sheet resets it.

Purpose: Implements Phase 3 Success Criteria 1 and 3. Gives Phase 4's AI features a clean mount point in specific tabs rather than a 1,141-line scroll.
Output: Tabbed ProspectSheet, lifted sheetTab state in TerritoryPlanner, four GREEN tab tests, updated ProspectPage (same Tabs wrapper for consistency).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-component-decomposition-ux-polish/03-RESEARCH.md
@.planning/phases/03-component-decomposition-ux-polish/03-01-PLAN.md
@.planning/audits/CRITIQUE-APPWIDE.md
@./CLAUDE.md
@src/components/ProspectSheet.tsx
@src/components/ProspectPage.tsx
@src/components/TerritoryPlanner.tsx
@src/components/ui/tabs.tsx
@src/components/OpportunitySheet.tsx
@src/test/ProspectSheet.tab.test.tsx

<interfaces>
<!-- shadcn Tabs API (already installed at src/components/ui/tabs.tsx) -->
```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";

<Tabs value={activeTab} onValueChange={onTabChange} defaultValue="overview">
  <TabsList>
    <TabsTrigger value="overview">Overview</TabsTrigger>
    <TabsTrigger value="activity">Activity</TabsTrigger>
    <TabsTrigger value="contacts">Contacts</TabsTrigger>
    <TabsTrigger value="tasks">Tasks</TabsTrigger>
  </TabsList>
  <TabsContent value="overview">{/* ... */}</TabsContent>
  <TabsContent value="activity">{/* ... */}</TabsContent>
  <TabsContent value="contacts">{/* ... */}</TabsContent>
  <TabsContent value="tasks">{/* ... */}</TabsContent>
</Tabs>
```

<!-- ProspectSheet section map from 03-RESEARCH.md -->
Tab assignments:
  Overview: Account Details (lines 578-661), Location Notes (~1044-1051), Signals Section (~999-1009), AI Readiness Card (~1012-1015), Account Research / Research findings (~663-749)
  Activity: Log Activity widget (~752-802), Notes + RichTextEditor + note log (~822-863), Activity Timeline (~1017-1042)
  Contacts: Contacts list + add/edit form (~865-998)
  Tasks: Open Tasks list (~804-836, currently nested under Log Activity)

<!-- New ProspectSheet prop shape -->
interface ProspectSheetProps {
  // existing props unchanged...
  activeTab?: "overview" | "activity" | "contacts" | "tasks"; // default "overview" if undefined (for ProspectPage reuse)
  onTabChange?: (tab: string) => void;                        // optional; falls back to internal useState if undefined
}

<!-- TerritoryPlanner new state shape -->
const [sheetProspectId, setSheetProspectId] = useState<any>(null);
const [sheetTab, setSheetTab] = useState<string>("overview");

const handleSheetClose = () => {
  setSheetProspectId(null);
  setSheetTab("overview"); // reset on close per UX-02
};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert it.todo tab tests to runnable expectations (RED phase)</name>
  <files>src/test/ProspectSheet.tab.test.tsx</files>
  <behavior>
    Convert the `it.todo()` blocks from plan 01 into real `it()` blocks with concrete assertions:
    - Test D: Render ProspectSheet wrapped in TooltipProvider with a dummy prospect. Query `screen.getAllByRole("tab")` and assert exactly 4 tabs with names Overview, Activity, Contacts, Tasks
    - Test E: Render with `activeTab="activity"` prop. Assert the Activity panel is visible (e.g. a known-to-Activity text like "Log Activity" is in the DOM and Contacts-only text is NOT)
    - Test F: Render with `prospectId="p1" activeTab="activity"`. Rerender with `prospectId="p2" activeTab="activity"` (same controlled value). Assert Activity text still visible, Overview-only text not in primary panel. Then rerender with `activeTab="overview"` after a simulated onClose cycle and assert Overview is visible (proves close resets the parent-owned state)
    - Test G: Render with `onTabChange` spy. Click the "Contacts" tab trigger via `fireEvent.click`. Assert spy called with "contacts"

    All 4 tests MUST FAIL initially (ProspectSheet has no Tabs yet, no activeTab prop). This is the RED checkpoint that Task 2 turns GREEN.
  </behavior>
  <action>
    Open `src/test/ProspectSheet.tab.test.tsx`. Replace the four `it.todo()` blocks with full `it()` bodies matching the Behavior above.

    Implementation notes:
    - Build a `makeProspect()` factory returning a valid Prospect object (id, name, website, status="Prospect", industry="QSR / Fast Casual", outreach="Not Started", priority="", tier="", contacts: [], interactions: [], noteLog: [], tasks: [], lastTouched: null, locationCount: 10)
    - Wrapper: `const Wrapper = ({ children }) => <TooltipProvider>{children}</TooltipProvider>` — use `@/components/ui/tooltip`
    - For Test F, use React Testing Library's `rerender` to swap props while keeping the tree mounted (proves persistence via controlled-prop pattern)
    - For Test G, import `fireEvent` or `userEvent` — prefer userEvent if already used elsewhere; else fireEvent.click is fine
    - Do NOT add `activeTab` to ProspectSheet's type signature here — the tests will fail to compile or fail at runtime with "unknown prop," which is the intended RED state. Use `as any` cast if TS blocks the test file from running.
  </action>
  <verify>
    <automated>cd /Users/micahbank/territory-plan-buddy/.claude/worktrees/bold-ritchie-621340 &amp;&amp; bunx vitest run src/test/ProspectSheet.tab.test.tsx --reporter=verbose 2>&amp;1 | tee /tmp/tab-red.log; grep -q "4 fail\|Tests  4 failed\|× renders 4 tab triggers" /tmp/tab-red.log &amp;&amp; echo "RED confirmed" || echo "RED not achieved"</automated>
  </verify>
  <done>
    All 4 tests (D, E, F, G) run and FAIL with assertion errors (not syntax errors). Output contains "RED confirmed". This is the prerequisite for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap ProspectSheet content in Tabs + lift sheetTab to TerritoryPlanner (GREEN phase)</name>
  <files>src/components/ProspectSheet.tsx, src/components/TerritoryPlanner.tsx, src/components/ProspectPage.tsx</files>
  <behavior>
    - All 4 tab tests (D, E, F, G) from Task 1 PASS
    - All existing ProspectSheet behaviors unchanged (inline edits, add contact, log activity, Draft Email, Meeting Prep still work)
    - Switching prospects from the table keeps the active tab (SC-3 of ROADMAP Phase 3)
    - Closing the sheet and reopening shows Overview (UX-02)
    - ProspectPage (the full-page variant) renders tabs too so the two surfaces stay consistent (visual smoke only — no separate test required for ProspectPage)
  </behavior>
  <action>
    **1. `src/components/ProspectSheet.tsx` — wrap content in Tabs**

    - Add import: `import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";`
    - Extend `ProspectSheetProps`:
      ```ts
      activeTab?: string;         // controlled value; defaults to internal state if undefined
      onTabChange?: (t: string) => void;
      ```
    - In the component body, accept the two new props. Add fallback internal state so ProspectPage (which passes no controlled tab) still works:
      ```tsx
      const [internalTab, setInternalTab] = useState<string>("overview");
      const tab = activeTab ?? internalTab;
      const setTab = onTabChange ?? setInternalTab;
      ```
    - Locate the "Content" section comment (line ~576). Inside `sheetContent`, after the sticky header block ends (line ~574), replace the opening `<div className="px-6 py-5 space-y-5">` with a `<Tabs value={tab} onValueChange={setTab} className="px-6 py-5">` wrapper.
    - Insert a `<TabsList className="grid grid-cols-4 w-full">` with four `<TabsTrigger>` entries: values `overview`, `activity`, `contacts`, `tasks`; labels with first-letter capitalized.
    - Move existing section JSX into four `<TabsContent value="...">` panels per the section map in the interfaces block above. IMPORTANT: do NOT duplicate or delete any section — each currently-rendered section lands in exactly one panel.
    - Keep the sub-Dialogs (Draft Email, Meeting Prep, ContactPickerDialog) OUTSIDE the Tabs block (they are modal overlays and should render regardless of active tab). They currently sit after the main content div — preserve that placement.
    - Remove per-section `animate-fade-in-up` stagger `style={{ animationDelay: "...ms" }}` attributes. Keep the class itself on top-level panel children so each panel fades in on mount (per 03-RESEARCH.md recommendation).

    **2. `src/components/TerritoryPlanner.tsx` — lift tab state**

    - Near the existing `const [sheetProspectId, setSheetProspectId] = useState<any>(null);` declaration (around line 472), add:
      ```tsx
      const [sheetTab, setSheetTab] = useState<string>("overview");
      ```
    - Find every site that closes the sheet (look for `setSheetProspectId(null)`). Wrap in a helper or add a sibling reset:
      ```tsx
      const handleSheetClose = useCallback(() => {
        setSheetProspectId(null);
        setSheetTab("overview");
      }, []);
      ```
    - Find the `<ProspectSheet ... />` render site. Pass:
      ```tsx
      activeTab={sheetTab}
      onTabChange={setSheetTab}
      onClose={handleSheetClose}
      ```
      Replace existing `onClose={() => setSheetProspectId(null)}` if present.

    **3. `src/components/ProspectPage.tsx` — consistency pass**

    - The full-page variant renders the same section groups. Since ProspectSheet now owns the Tabs structure, verify ProspectPage either:
      (a) delegates to ProspectSheet's internals (unlikely — separate component), OR
      (b) needs its own Tabs wrapper.
    - If (b): mirror the same four-tab structure in ProspectPage.tsx using the same section mapping. Use a local `useState("overview")` (no persistence requirement for the full-page route).
    - Keep this change MINIMAL — if ProspectPage already has some differing layout, don't blow it up. The goal is IA consistency; full parity can land in a follow-up if scope creeps.

    **4. Verify no regression in critical flows**

    - Dashboard Quota strip, BulkOutreach, ContactPicker — not touched, should not regress
    - Phase 04 Draft Emails badge still opens PendingOutreachDialog (which sits outside ProspectSheet — unaffected)
    - Phase 04 bulk Mark Contacted — uses bulkConfirm state in TerritoryPlanner, untouched
  </action>
  <verify>
    <automated>cd /Users/micahbank/territory-plan-buddy/.claude/worktrees/bold-ritchie-621340 &amp;&amp; bunx vitest run src/test/ProspectSheet.tab.test.tsx src/test/ProspectSheet.responsive.test.tsx --reporter=verbose &amp;&amp; bunx tsc --noEmit &amp;&amp; bunx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - All 4 tab tests PASS (UX-01 + UX-02 covered)
    - Responsive tests from plan 01 still PASS
    - `bunx tsc --noEmit` clean
    - Full suite clean
    - Manual smoke: open prospect A, click Activity tab, click prospect B in the table → sheet switches to B's content, Activity tab still active. Close sheet. Click prospect C → sheet opens on Overview. All four tab panels render the expected subsections from the research map.
  </done>
</task>

</tasks>

<verification>
- `bunx vitest run` — full suite green
- `bunx tsc --noEmit` — clean
- Manual: exercise tab persistence across prospect switches (SC-3), confirm reset on close (SC-1), verify each tab shows the right sections
- Phase 04 regression check: Draft Emails badge still shows, PendingOutreachDialog still opens, bulk Mark Contacted still logs + bumps stages
</verification>

<success_criteria>
- UX-01 complete: Four tabs render in ProspectSheet
- UX-02 complete: Tab persists across prospect switch; resets on close
- Phase 3 Success Criteria 1 and 3 satisfied (measurable via passing Tests D, F)
- No regression in Phase 01 rollback semantics or Phase 04 features
</success_criteria>

<output>
After completion, create `.planning/phases/03-component-decomposition-ux-polish/03-02-SUMMARY.md` documenting: tab structure shipped, sheetTab state ownership, any section placement tweaks vs the research map, test pass confirmation, ProspectPage changes (if any).
</output>
